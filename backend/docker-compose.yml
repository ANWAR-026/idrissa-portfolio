version: '3.8'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=development
      - FRONTEND_URL=http://localhost:3000
    volumes:
      - ./src:/app/src
    command: npm run dev
    restart: unless-stopped
